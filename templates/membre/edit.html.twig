{% extends 'base.html.twig' %}

{% block title %}
    {% if is_granted('ROLE_ADMIN') %}
        MODIFIER Le Membre
    {% else %}
        MODIFIER Mon profil
    {% endif %}
{% endblock %}

{% block body %}
    <h1>{{ is_granted("ROLE_ADMIN")?"Modifier" :"Modifier mon Profil" }}</h1>

    {{ include('membre/_form.html.twig', {'button_label': 'Update'}) }}
    {% if is_granted("ROLE_ADMIN") %}
        <a href="{{ path('membre_index') }}">retour à la liste</a>
    {% endif %}
    {{ include('membre/_delete_form.html.twig')}}
{% endblock %}
